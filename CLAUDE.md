@AGENTS.md

## Design Context

This project has `PRODUCT.md` (strategy: users, purpose, register/platform, brand personality) and `DESIGN.md` (visual system: colors, typography, components) at the repo root. Read both before any UI work — `/impeccable` commands (craft, critique, audit, polish, etc.) load them automatically. In short: a field-ops scouting console for FRC Team 5806, maroon + graphite, flat/bordered (no shadows), numeric data always in monospace.
